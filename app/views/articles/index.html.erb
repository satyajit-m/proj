<div class="container-fluid col-md-4 text-md-center">
    <%= link_to 'New article', new_article_path, class:  "btn btn-primary" %>
    <h1>All Articles</h1>

    <table class="table">
        <tr>
            <th scope="col">Id</th>
            <th cope="col">Title</th>
            <th scope="col">Text</th>
            <th></th>
        </tr>

        <% @articles.each do |article| %>
        <tr>
            <th scope="row"><%= article.id %></td>
            <td><%= article.title %></td>
            <td><%= article.text %></td>
            <td><%= link_to 'Show', article_path(article), class:  "btn btn-dark" %></td>
        </tr>
        <% end %>
    </table>
    <div>